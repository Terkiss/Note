소워, 딸ㅋ, 잠

class Name -> PeekabooList

변수 이름 ->
         데이터를 관리하는 클래스 변수  변수 
         private Head 시작 주소 == a[0];
         private head = new PeekabooNode();
         private CurrentInorderNode  = null
         사이즈 변수 Count = 0  -- 삽입시   Count++, 
                             삭제시   Count--
        



함수 이름 

        equal(a, b) 비교 함수 같으면 0,
                    a가 크면 -1 ,
                    b가 크면 +1
        add(data) 입력 데이터를 리스트에 저장
        remove(index) 인덱스를 이용 해당 번쨰의 데이터를 삭제

        removeAll() 모든 데이터 삭제  
                예외 Count == 0 삭제 
                        ex if-> count ->> 0 
                                return ; 

        size() 사이즈 출력 Size(){ return Count }

        get(index) 해당 인덱스의 데이터를 출력
                CurrentInorderNode = Head.next
                if(CurrentInorderNode == null )
                {
                        print -> 비엇습니다 뭐든 넣고 호출
                        return;
                }
                for(int i = 0 ; i == index, i++)
                {
                     
                        CurrentInorderNode <- CurrentInorderNode.next
                
                }
                return CurrentInorderNode.data
        get() 리스트내 모든 데이터 출력 


        6장 -.> >>>>>>>>>>>>>>>>>>>> 오버로딩
        10장 
         



PeekabooNode
        필드명
        Next -> PeekabooNode type 
        data -> 0 x 
